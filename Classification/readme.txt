I put all the images in the 'result' folder. And you can see 'run time' and 'performance' in result.txt, which is also in the 'result' folder.
①You can run code in '.ipynb' module by module.
②For Classifier models and dimension reduction methods, I suggest you read my comment in the module, now I'm using KNN + PCA, You can run other models by copying the code in the comments.
